################ Uncomment a variable to change. All of the provided values are defaults. ################

# APP_TITLE=YT-DLP Player
# THEME_COLOR="#ff7300"
# GENERATE_SPRITE_BELOW=1800 # generate sprites for videos below this duration (0 to disable)
# AMOLED_BG=false
# MAX_VIDEO_AGE=3600 # inactivity after which video is removed
# MAX_VIDEO_DURATION=36000
# DEFAULT_QUALITY=720
# MAX_QUALITY=2160
# AUTOPLAY=false
# MIN_LIVE_BUFFER=1 # prevent livestreams from having less buffer than this value, in seconds (0 to disable)
# ALWAYS_TRANSCODE=false
# DISABLE_TRANSCODING=false # useful for low-performance machines, only basic features will work
# MAX_PROCESSES=5 # number of FFMPEG and YT-DLP processes combined, allowed to run at once
# AUTOSKIP_SB_SEGMENTS= # comma separated, e.g. selfpromo,outro,sponsor,preview,interaction,intro,music_offtopic
# COOKIES_ONLY_ON_FAILURE=true # only use cookies if necessary
# PLAYLIST_SUPPORT=true
# AUTO_BG_PLAYBACK=true # keep playing videos in background after leaving application or locking screen
# AUDIO_VISUALIZER=false
# DATA_PATH=./data
# SAVE_ALL=true # keep every downloaded video permanently (disables MAX_VIDEO_AGE cleanup)
# API_KEY= # if set, every request to /api/v1/* must include X-API-Key header
# YTDLP_SEARCH_DEFAULT_N=24 # default result count when a search prefix omits it (e.g. `ytsearch:cats` -> `ytsearch24:cats`). Set to `all` for unlimited.
# SEARCH_CACHE_TTL=3600 # seconds to cache search results in-process (set 0 to disable)
# THUMB_PROXY_ALLOW_ANY=false # if true, /thumb-proxy accepts any host; default only allows common CDN suffixes (phncdn, ytimg, twimg, etc.)
# PROXY="" # proxy for accessing video sources, e.g. http://127.0.0.1:8888
# PORT = 5000